const numbers = ['one' , 'Two' , 'Three' , 'Four' ,'Five']
/*


		console.log(numbers)
		console.log(numbers.length)
		// start
		numbers.shift()
		console.log(numbers)
		console.log(numbers.length)


		numbers.unshift('new' , "inserted")
		console.log(numbers)
		console.log(numbers.length)

		// end
		numbers.pop()
		console.log(numbers)
		console.log(numbers.length)


		numbers.push('new' , "inserted")
		console.table(numbers)
		console.table(numbers.length)

		*/

// Middle
numbers.splice(2,2,'Something',"2nd add43ed","THIRD")
console.table(numbers)

// var result = ""
// for (let i = 20; i >=1; i--) {
	
// 	if (i%5==0) {
// 		console.log("\n")
// 	}
// 	console.log(`${result}${i}\t`)
 
// }